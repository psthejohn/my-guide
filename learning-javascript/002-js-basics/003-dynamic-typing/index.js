// there are two type of languages 
// static and dynamic language

// in static language if the type of that variable is said once , it can not be changed later but in dyanamic type of vairable can change at runtime

let name = 'ashu';

console.log(typeof  name); // output String

name = 1;

console.log(typeof name); //output Number
