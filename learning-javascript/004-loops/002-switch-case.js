let role = 'guest';

switch (role) {
	case 'guest':
		console.log('Guest User');
		break;

	case 'moderator':
		console.log('Moderator User');
		break;

	default:
		console.log('Unknown User');
}


// we can use if else also to achieve the same functionality
// switch-case is a bit outdated so most ofter if else is used only 
